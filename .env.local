# Configuração local de desenvolvimento
# Este arquivo não é versionado (está no .gitignore)
# Porta 5000 = porta do docker-compose (mapeamento host:container = 5000:5000)
VITE_API_BASE_URL=http://localhost:5000
